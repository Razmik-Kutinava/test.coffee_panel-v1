# ⚙️ Критично! Настройка Root Directory в Vercel

## 🔧 Что нужно сделать в Vercel Dashboard ПРЯМО СЕЙЧАС:

### Шаг 1: Измените Root Directory

1. Откройте: https://vercel.com/razmik-giurdzhians-projects/test-coffee-panel-v1/settings
2. Прокрутите до секции **"Build & Development Settings"**
3. Найдите поле **"Root Directory"**
4. Введите: `frontend`
5. Нажмите **"Save"**

### Шаг 2: Проверьте остальные настройки

После установки Root Directory = `frontend`, Vercel должен автоматически определить:

```
Framework Preset:       Vite
Build Command:          npm run build
Output Directory:       dist
Install Command:        npm install
```

**Если не определил автоматически**, установите вручную:

```
Framework Preset:       Other
Build Command:          npm run build
Output Directory:       dist  
Install Command:        npm install
```

### Шаг 3: Добавьте переменную окружения

1. Откройте: https://vercel.com/razmik-giurdzhians-projects/test-coffee-panel-v1/settings/environment-variables
2. Нажмите **"Add New Variable"**

```
Key:          VITE_API_URL
Value:        http://localhost:3001
Environment:  ✅ Production  ✅ Preview  ✅ Development
```

**⚠️ Важно:** Пока оставьте `localhost:3001`. После деплоя backend на Railway обновите!

### Шаг 4: Redeploy

1. Перейдите: https://vercel.com/razmik-giurdzhians-projects/test-coffee-panel-v1
2. Найдите последний deployment
3. Нажмите **⋯ (три точки)** → **"Redeploy"**
4. Выберите **"Redeploy without cache"**

---

## ✅ Что должно получиться:

### Build Logs должны показать:

```
Running build in Washington, D.C., USA
Cloning github.com/Razmik-Kutinava/test.coffee_panel-v1 (main, 7d526ec)
Cloning completed: ~700ms

Detected Vite
Running "npm install"
added 245 packages in 4.2s

Running "npm run build"
> vite build

vite v7.1.4 building for production...
✓ 487 modules transformed.
dist/index.html                      1.23 kB │ gzip:  0.56 kB
dist/assets/index-[hash].js        245.67 kB │ gzip: 78.45 kB

✓ built in 8.34s

Build Completed in /vercel/output [10245ms]
Deploying outputs...
Deployment completed
```

**Время:** 10-15 секунд (НЕ 127ms!)

---

## 📸 Скриншот настроек:

```
┌─────────────────────────────────────────────────────┐
│ Build & Development Settings                        │
├─────────────────────────────────────────────────────┤
│                                                      │
│ Framework Preset                                    │
│ ┌──────────────────────────────────────┐            │
│ │ Vite                                 │ ▼          │
│ └──────────────────────────────────────┘            │
│                                                      │
│ Root Directory                                      │
│ ┌──────────────────────────────────────┐            │
│ │ frontend                             │            │
│ └──────────────────────────────────────┘            │
│ ⚠️ Build Command, Output Directory,                 │
│    and Install Command will be relative             │
│    to this directory.                               │
│                                                      │
│ Build Command                                       │
│ ┌──────────────────────────────────────┐            │
│ │ npm run build                        │            │
│ └──────────────────────────────────────┘            │
│                                                      │
│ Output Directory                                    │
│ ┌──────────────────────────────────────┐            │
│ │ dist                                 │            │
│ └──────────────────────────────────────┘            │
│                                                      │
│ Install Command                                     │
│ ┌──────────────────────────────────────┐            │
│ │ npm install                          │            │
│ └──────────────────────────────────────┘            │
│                                                      │
│                                        [Save]       │
└─────────────────────────────────────────────────────┘
```

---

## 🎯 Почему это работает:

1. **Root Directory = `frontend`** → Vercel работает внутри папки `frontend/`
2. **Build Command = `npm run build`** → выполняется `vite build` из `frontend/package.json`
3. **Output Directory = `dist`** → относительно `frontend/`, т.е. `frontend/dist`
4. **vercel.json** → только для rewrites и headers (не для build настроек)

Это стандартный подход для монорепо в Vercel! 

---

## 🐛 Если всё равно не работает:

### Попробуйте альтернативный метод:

1. Удалите `vercel.json` из корня (временно переименуйте в `vercel.json.bak`)
2. В Vercel Dashboard установите только:
   - Root Directory: `frontend`
   - Framework: Vite (или Other)
3. Redeploy

Vercel должен автоматически всё определить для Vite проекта.

---

## 📞 Проверка после деплоя:

1. Откройте https://test-coffee-panel-v1.vercel.app
2. Должен загрузиться Admin Hub (Dashboard)
3. DevTools → Console → не должно быть критичных ошибок
4. DevTools → Network → запросы идут к `VITE_API_URL`

Если видите белый экран → посмотрите Console для ошибок!

---

**Прямые ссылки:**
- Settings: https://vercel.com/razmik-giurdzhians-projects/test-coffee-panel-v1/settings
- Deployments: https://vercel.com/razmik-giurdzhians-projects/test-coffee-panel-v1

🚀 После настройки Root Directory деплой должен заработать правильно!

