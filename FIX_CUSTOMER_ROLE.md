# 🔧 Исправление ошибки с ролью 'customer'

## ✅ Что уже сделано

1. ✅ Добавлена временная защита в код (обработка ошибок в `users.service.ts` и `orders.service.ts`)
2. ✅ Код теперь автоматически нормализует роль `customer` → `client` при загрузке данных
3. ✅ Приложение должно работать даже если в enum нет значения `customer`

## 🎯 Что нужно сделать для полного исправления

### Шаг 1: Добавить значение в базу данных (ОБЯЗАТЕЛЬНО)

Выполните в **Supabase SQL Editor** (по одному запросу):

```sql
ALTER TYPE "UserRole" ADD VALUE IF NOT EXISTS 'customer';
```

Проверьте результат:

```sql
SELECT unnest(enum_range(NULL::"UserRole")) AS role_value;
```

Должно быть видно `customer` в списке.

### Шаг 2: Обновить продакшн на Render

#### Вариант A: Через Git (рекомендуется)

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "fix: add customer role support and error handling"
   git push
   ```

2. Render автоматически пересоберет и перезапустит сервис

#### Вариант B: Через Render Dashboard

1. Откройте Render Dashboard → ваш сервис
2. Settings → Build Command
3. Убедитесь, что есть: `cd backend && npx prisma generate && npm run build`
4. Manual Deploy → Deploy latest commit

### Шаг 3: Проверка

После деплоя проверьте:

```bash
curl https://test-coffee-panel-v1.onrender.com/users
curl https://test-coffee-panel-v1.onrender.com/orders
```

Не должно быть ошибки 500.

## 📝 Что было изменено в коде

### `backend/src/users/users.service.ts`
- Добавлена обработка ошибки при загрузке пользователей
- Автоматическая нормализация роли `customer` → `client`
- Fallback на raw SQL при ошибке enum

### `backend/src/orders/orders.service.ts`
- Добавлена обработка ошибки при загрузке заказов
- Автоматическая нормализация роли пользователя `customer` → `client`
- Fallback на загрузку пользователей через raw SQL при ошибке enum

## ⚠️ Важно

**Временная защита работает, но для полного исправления нужно:**

1. ✅ Добавить `customer` в enum в базе данных (Шаг 1)
2. ✅ Перегенерировать Prisma Client на продакшене (Шаг 2)
3. ✅ Перезапустить сервис (Шаг 2)

После этого временная защита больше не понадобится, но она останется для обратной совместимости.

## 🔍 Проверка Telegram бота и Flutter приложения

### Telegram бот
- Если бот использует API бэкенда → должно работать автоматически
- Если бот обращается напрямую к БД → убедитесь, что enum обновлен

### Flutter мини-апп
- Проверьте, что типы ролей включают `customer`:
  ```dart
  enum UserRole {
    client,
    customer,  // должно быть
    barista,
    // ...
  }
  ```
- Если приложение получает роли через API → должно работать автоматически

## 📞 Если проблема сохраняется

1. Проверьте логи на Render Dashboard
2. Убедитесь, что значение добавлено в enum (Шаг 1)
3. Убедитесь, что сервис перезапущен после деплоя
4. Проверьте, что Prisma Client перегенерирован (должна быть строка в логах сборки)

