# Статус загрузки изображений для товаров

## ✅ ВСЁ ГОТОВО! Функционал полностью реализован

Весь код для загрузки изображений товаров уже написан и настроен. Вам остается только создать bucket в Supabase Dashboard и протестировать.

---

## 📋 Что уже реализовано

### 1. **Backend** ✅

#### StorageService (backend/src/storage/)
- ✅ Сервис для работы с Supabase Storage
- ✅ Загрузка изображений с валидацией (тип, размер до 5MB)
- ✅ Удаление изображений
- ✅ Генерация уникальных имен файлов
- ✅ Получение публичных URL

**Расположение:** `backend/src/storage/storage.service.ts`

#### ProductsController
- ✅ Endpoint `POST /products/upload-image` для загрузки изображений
- ✅ Интеграция с StorageService
- ✅ FileInterceptor для обработки multipart/form-data

**Расположение:** `backend/src/products/products.controller.ts:89-110`

#### ProductsService
- ✅ Автоматическое удаление старых изображений при обновлении товара
- ✅ Удаление изображений при удалении товара

**Расположение:** `backend/src/products/products.service.ts`

### 2. **Frontend** ✅

#### Catalog.tsx
- ✅ UI для выбора изображения (input type="file")
- ✅ Превью изображения перед загрузкой
- ✅ Валидация типа файла (JPEG, PNG, WebP, GIF)
- ✅ Валидация размера файла (макс 5MB)
- ✅ Отображение загруженных изображений в списке товаров
- ✅ Возможность удалить изображение
- ✅ Индикатор загрузки

**Расположение:** `frontend/src/pages/Catalog.tsx:768-847`

#### useApi.ts
- ✅ Функция `uploadProductImage()` для отправки файла на сервер
- ✅ Обработка ошибок

**Расположение:** `frontend/src/hooks/useApi.ts:144-156`

### 3. **Конфигурация** ✅

#### .env файл
- ✅ `SUPABASE_URL` настроен
- ✅ `SUPABASE_SERVICE_KEY` добавлен (строка 13 в .env)
- ✅ База данных настроена

**Расположение:** `backend/.env`

---

## 🚀 Что нужно сделать

### Шаг 1: Создать bucket в Supabase Dashboard

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект: `wntvxdgxzenehfzvorae`
3. Перейдите в **Storage** (в левом меню)
4. Нажмите **"New bucket"** или **"Create bucket"**
5. Заполните форму:
   - **Name**: `product-images` (важно! именно это имя)
   - **Public bucket**: ✅ **Включите** (чтобы изображения были доступны публично)
   - **File size limit**: `5 MB`
   - **Allowed MIME types**: `image/jpeg, image/png, image/webp, image/gif`
6. Нажмите **"Create bucket"**

### Шаг 2: Настроить политики доступа (RLS Policies)

После создания bucket настройте политики:

1. В Storage → `product-images` → **Policies**
2. Создайте политику для чтения (публичный доступ):

```sql
-- Публичное чтение изображений
CREATE POLICY "Public Access for product images"
ON storage.objects
FOR SELECT
USING (bucket_id = 'product-images');
```

3. Создайте политику для загрузки (для тестирования можно сделать публичной):

```sql
-- Публичная загрузка изображений (для тестирования)
CREATE POLICY "Public upload for product images"
ON storage.objects
FOR INSERT
WITH CHECK (bucket_id = 'product-images');
```

4. Создайте политику для удаления:

```sql
-- Публичное удаление изображений (для тестирования)
CREATE POLICY "Public delete for product images"
ON storage.objects
FOR DELETE
USING (bucket_id = 'product-images');
```

⚠️ **Важно:** Для production рекомендуется ограничить доступ только авторизованным пользователям. См. `backend/SUPABASE_STORAGE_SETUP.md` для деталей.

### Шаг 3: Обновить Node.js для frontend (опционально)

⚠️ **Предупреждение:** Frontend требует Node.js 20.19+ или 22.12+, а у вас установлен 20.11.0.

Если хотите запустить frontend локально:

```bash
nvm install 20.19.0
nvm use 20.19.0
cd frontend
npm run dev
```

---

## 🧪 Как протестировать

### Вариант 1: Локально (после обновления Node.js)

1. Запустите серверы:
   ```bash
   # Backend (уже запущен)
   cd backend
   npm run start:dev

   # Frontend
   cd frontend
   npm run dev
   ```

2. Откройте http://localhost:3000
3. Перейдите в **Каталог** → **Товары**
4. Нажмите **"Добавить товар"** или отредактируйте существующий
5. Выберите изображение через кнопку "Выбрать файл"
6. Заполните остальные поля и нажмите **"Создать"**
7. Изображение загрузится в Supabase Storage и отобразится в списке товаров

### Вариант 2: На production (Vercel)

1. Убедитесь, что bucket `product-images` создан в Supabase
2. Убедитесь, что переменные окружения настроены на Vercel:
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_KEY`
3. Откройте https://test-coffee-panel-v1.vercel.app/
4. Попробуйте создать товар с изображением

---

## 📊 Архитектура загрузки изображений

```
┌─────────────┐
│   Browser   │
│  (Frontend) │
└──────┬──────┘
       │ 1. Выбор файла
       │ 2. Отправка FormData
       ▼
┌────────────────────────┐
│  ProductsController    │
│  POST /products/       │
│  upload-image          │
└──────────┬─────────────┘
           │ 3. FileInterceptor
           │ 4. Валидация
           ▼
┌────────────────────────┐
│   StorageService       │
│  uploadImage(file)     │
└──────────┬─────────────┘
           │ 5. Upload to Supabase
           ▼
┌────────────────────────┐
│  Supabase Storage      │
│  Bucket: product-images│
└──────────┬─────────────┘
           │ 6. Return public URL
           ▼
┌────────────────────────┐
│   Database (Product)   │
│  imageUrl: "https://..." │
└────────────────────────┘
           │
           ▼
┌────────────────────────┐
│   Flutter App          │
│  Displays image        │
└────────────────────────┘
```

---

## 🔍 Проверка работы

### 1. Проверить, что backend видит Supabase Storage

Backend логи должны НЕ содержать предупреждения:
```
⚠️ SUPABASE_URL или SUPABASE_SERVICE_KEY не настроены
```

Если видите это предупреждение - перезапустите backend после настройки .env.

### 2. Проверить endpoint загрузки

```bash
curl -X POST http://localhost:3001/products/upload-image \
  -F "image=@/path/to/your/image.jpg"
```

Ожидаемый ответ:
```json
{
  "imageUrl": "https://wntvxdgxzenehfzvorae.supabase.co/storage/v1/object/public/product-images/product-1234567890-123456789.jpg",
  "filename": "image.jpg",
  "size": 12345,
  "mimetype": "image/jpeg"
}
```

### 3. Проверить загруженное изображение

Откройте URL из `imageUrl` в браузере - изображение должно отобразиться.

---

## 🐛 Troubleshooting

### Ошибка: "Supabase Storage не настроен"

**Причина:** Переменные `SUPABASE_URL` или `SUPABASE_SERVICE_KEY` не определены.

**Решение:**
1. Проверьте `backend/.env`
2. Убедитесь, что переменная `SUPABASE_SERVICE_KEY` присутствует (строка 13)
3. Перезапустите backend

### Ошибка: "Bucket not found"

**Причина:** Bucket `product-images` не создан в Supabase.

**Решение:**
1. Откройте Supabase Dashboard → Storage
2. Создайте bucket с именем `product-images`
3. Убедитесь, что имя точно совпадает (без пробелов, маленькие буквы)

### Ошибка: "new row violates row-level security policy"

**Причина:** Политики доступа не настроены.

**Решение:**
1. Откройте Supabase Dashboard → Storage → `product-images` → Policies
2. Создайте политики доступа (см. Шаг 2 выше)

### Изображения не отображаются

**Причина:** Bucket не помечен как публичный.

**Решение:**
1. Откройте Supabase Dashboard → Storage → `product-images`
2. Убедитесь, что **Public bucket** включен
3. Проверьте политики доступа

---

## 📝 Примечания

- Изображения загружаются с уникальными именами: `product-{timestamp}-{random}.{extension}`
- Максимальный размер файла: 5MB
- Поддерживаемые форматы: JPEG, PNG, WebP, GIF
- При обновлении товара старое изображение автоматически удаляется
- При удалении товара изображение также удаляется из Storage

---

## 🔐 Безопасность для Production

⚠️ **Важно:** Текущие настройки подходят для тестирования. Для production:

1. **Ограничьте загрузку/удаление** только авторизованным пользователям
2. Используйте JWT токены для аутентификации
3. **Не используйте** `SUPABASE_SERVICE_KEY` на frontend!
4. Регулярно ротируйте ключи доступа
5. Настройте rate limiting

См. `backend/SUPABASE_STORAGE_SETUP.md` для деталей.

---

## ✅ Итого

**Все готово для использования!** Нужно только:
1. ✅ Создать bucket `product-images` в Supabase
2. ✅ Настроить политики доступа
3. ✅ Протестировать загрузку

**Backend работает на:** http://localhost:3001
**Frontend будет на:** http://localhost:3000 (после обновления Node.js)
**Production:** https://test-coffee-panel-v1.vercel.app/

Вопросы? См. `backend/SUPABASE_STORAGE_SETUP.md` для деталей.
