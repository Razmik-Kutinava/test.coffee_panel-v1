# 🔧 Исправление предупреждения "Custom domains won't be assigned"

## ⚠️ Что это значит:

**"Custom domains won't be assigned—auto-assignment is disabled"** — это **НЕ ошибка**, а просто предупреждение.

Это означает, что если вы добавите кастомный домен (например, `coffee-panel.com`), Vercel не будет автоматически назначать его на новые деплои.

---

## ✅ Это нормально для v1.0!

Для текущей версии это **не критично**, потому что:
- ✅ Проект работает на стандартном домене Vercel: `test-coffee-panel-v1.vercel.app`
- ✅ Все функции работают нормально
- ✅ Это не влияет на работу приложения

---

## 🔧 Если хотите включить auto-assignment (опционально):

### Шаг 1: Откройте настройки проекта

1. Перейдите: https://vercel.com/razmik-giurdzhians-projects/test-coffee-panel-v1/settings
2. Найдите секцию **"Domains"** или **"Deployment Settings"**

### Шаг 2: Включите Auto-assignment

1. Найдите опцию **"Auto-assign Custom Domains"** или **"Automatic Domain Assignment"**
2. Включите переключатель
3. Сохраните изменения

---

## 📋 Другие возможные проблемы (проверьте):

### 1. Root Directory установлен?

**Проверьте:**
1. https://vercel.com/razmik-giurdzhians-projects/test-coffee-panel-v1/settings
2. **Root Directory** должно быть: `frontend`
3. Если нет — установите и сохраните

### 2. Build Command правильный?

**Проверьте:**
1. В настройках проекта
2. **Build Command** должно быть: `npm run build` (без `cd frontend`, так как Root Directory уже установлен)
3. Если указано `cd frontend && npm run build` — уберите `cd frontend &&`

### 3. Output Directory правильный?

**Проверьте:**
1. **Output Directory** должно быть: `dist`
2. НЕ `frontend/dist` (так как Root Directory = `frontend`)

### 4. Environment Variables установлены?

**Проверьте:**
1. https://vercel.com/razmik-giurdzhians-projects/test-coffee-panel-v1/settings/environment-variables
2. Должна быть переменная:
   ```
   VITE_API_URL = https://test-coffee-panel-v1.onrender.com
   ```

---

## 🎯 Итого:

**Предупреждение о custom domains можно игнорировать** — это не влияет на работу приложения.

Если хотите убрать предупреждение:
1. Включите auto-assignment в настройках (опционально)
2. Или просто игнорируйте — это не критично

---

## ✅ Проверка что всё работает:

1. **Откройте:** https://test-coffee-panel-v1.vercel.app
2. **Должен загрузиться:** Admin Hub Dashboard
3. **DevTools → Console:** Нет критичных ошибок
4. **DevTools → Network:** Запросы идут на `https://test-coffee-panel-v1.onrender.com`

Если всё это работает — **предупреждение можно игнорировать!** 🎉

