# 🚨 КРИТИЧНО: Настройка переменных окружения на Render

## Проблема

Ошибка при загрузке изображений:
```
❌ Ошибка загрузки изображения: Supabase Storage не настроен. 
Проверьте переменные окружения SUPABASE_URL и SUPABASE_SERVICE_KEY.
```

**Причина:** На Render не настроены переменные окружения для Supabase Storage.

---

## ✅ Решение: Добавить переменные на Render Dashboard

### Шаг 1: Откройте Render Dashboard

1. Перейдите на https://dashboard.render.com
2. Войдите в свой аккаунт
3. Найдите сервис **coffee-panel-backend**
   - Или сервис, который обслуживает `test-coffee-panel-v1.onrender.com`

### Шаг 2: Откройте настройки Environment

1. В меню сервиса выберите **Environment** (или **Environment Variables**)
2. Нажмите **Add Environment Variable**

### Шаг 3: Добавьте переменные

**Добавьте 2 переменные:**

#### Переменная 1: SUPABASE_URL

```
Key:   SUPABASE_URL
Value: https://wntvxdgxzenehfzvorae.supabase.co
```

#### Переменная 2: SUPABASE_SERVICE_KEY

```
Key:   SUPABASE_SERVICE_KEY
Value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndudHZ4ZGd4emVuZWhmenZvcmFlIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NTExNDEwOCwiZXhwIjoyMDgwNjkwMTA4fQ.xea_k8DBEUjPO1ThPGgwxkAwsH2SnRIgxfiPpRhy9kk
```

**⚠️ ВАЖНО:**
- Названия переменных должны быть ТОЧНО такими (регистр важен!)
- Значения должны быть БЕЗ кавычек
- Не должно быть пробелов в начале или конце

### Шаг 4: Сохраните и дождитесь redeploy

1. Нажмите **Save Changes**
2. Render автоматически запустит redeploy
3. Дождитесь завершения деплоя (обычно 2-5 минут)

---

## 🧪 Проверка после настройки

### 1. Проверьте логи Render

После деплоя откройте логи сервиса и найдите:

**✅ Правильно (переменные настроены):**
```
[Nest] LOG [StorageService] StorageService initialized
[Nest] LOG [ProductsModule] ProductsModule dependencies initialized
```

**❌ Неправильно (переменные НЕ настроены):**
```
[Nest] WARN [StorageService] ⚠️ SUPABASE_URL или SUPABASE_SERVICE_KEY не настроены
[Nest] WARN [StorageService] Загрузка изображений будет недоступна
```

### 2. Протестируйте загрузку изображения

1. Откройте https://test-coffee-panel-v1.vercel.app/
2. Перейдите в **Каталог** → **Товары** → **➕ Добавить товар**
3. Нажмите **📸 Выбрать изображение**
4. Выберите JPEG/PNG файл
5. Заполните остальные поля (название, цена)
6. Нажмите **Создать**

**✅ Ожидаемый результат:**
- Товар создается успешно
- Изображение загружается в Supabase Storage
- Товар отображается с картинкой в списке
- Никаких ошибок 400 Bad Request

---

## 📋 Чеклист

Отметьте каждый пункт после выполнения:

- [ ] Открыл Render Dashboard
- [ ] Нашел сервис coffee-panel-backend
- [ ] Открыл раздел Environment
- [ ] Добавил переменную SUPABASE_URL
- [ ] Добавил переменную SUPABASE_SERVICE_KEY
- [ ] Нажал Save Changes
- [ ] Дождался завершения redeploy (2-5 минут)
- [ ] Проверил логи - нет предупреждений о Supabase
- [ ] Протестировал загрузку через панель
- [ ] Изображение успешно загрузилось
- [ ] Товар отображается с картинкой

---

## 🔍 Где взять значения переменных?

### Вариант 1: Из локального .env файла

Откройте `backend/.env` и скопируйте значения:
- `SUPABASE_URL` (строка 7)
- `SUPABASE_SERVICE_KEY` (строка 13)

### Вариант 2: Из Supabase Dashboard

1. Откройте https://supabase.com/dashboard
2. Выберите проект
3. Перейдите в **Settings** → **API**
4. Скопируйте:
   - **Project URL** → `SUPABASE_URL`
   - **service_role** key (секретный!) → `SUPABASE_SERVICE_KEY`

---

## ⚠️ Безопасность

**SUPABASE_SERVICE_KEY** - это **секретный ключ с полными правами!**

✅ **Правильно:**
- Хранить в Render Environment Variables (защищено)
- Использовать только на backend
- НЕ коммитить в .env в GitHub

❌ **Неправильно:**
- Использовать на frontend
- Публиковать в открытом репозитории
- Передавать через URL или cookies

---

## 🚀 После настройки

**Через 5 минут после добавления переменных:**

1. ✅ Backend увидит переменные
2. ✅ StorageService инициализирует Supabase клиент
3. ✅ Endpoint `/products/upload-image` заработает
4. ✅ Загрузка изображений заработает на production
5. ✅ Никаких ошибок 400 Bad Request

**Всё будет работать!** 🎉

---

## 📝 Если что-то не работает

### Проблема: Переменные добавлены, но всё равно ошибка 400

**Решение:**
1. Проверьте, что правильно скопировали значения (без пробелов в конце)
2. Убедитесь, что нажали "Save Changes"
3. Дождитесь завершения redeploy (может занять 5 минут)
4. Проверьте логи Render - должно быть сообщение о переменных
5. Убедитесь, что названия переменных ТОЧНО: `SUPABASE_URL` и `SUPABASE_SERVICE_KEY`

### Проблема: Не могу найти coffee-panel-backend на Render

**Решение:**
1. Возможно, сервис называется по-другому
2. Проверьте все сервисы в вашем Render аккаунте
3. Ищите сервис, который обслуживает `test-coffee-panel-v1.onrender.com`

### Проблема: Логи показывают предупреждение о Supabase

**Решение:**
1. Значит переменные не добавлены или добавлены неправильно
2. Проверьте названия переменных - должны быть ТОЧНО:
   - `SUPABASE_URL` (не Supabase_Url или supabase_url)
   - `SUPABASE_SERVICE_KEY` (не SUPABASE_KEY)
3. Проверьте значения - должны быть без кавычек
4. Убедитесь, что нажали "Save Changes" и дождались redeploy

---

## ✅ Готово!

После выполнения всех шагов:
- Backend на Render будет полностью настроен
- Загрузка изображений заработает
- Ошибка 400 Bad Request исчезнет
- Всё будет работать идеально!
