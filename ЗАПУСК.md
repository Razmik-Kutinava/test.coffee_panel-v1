# 🚀 Инструкция по запуску Coffee Hub

## 📋 Доступные скрипты

### 1️⃣ **start-backend-only.ps1** - Запуск только Backend
Запускает backend с x64 Node.js для совместимости ARM64

```powershell
.\start-backend-only.ps1
```

**Что делает:**
- ✅ Останавливает старые процессы backend
- ✅ Проверяет x64 Node.js
- ✅ Исправляет конфликты .env
- ✅ Регенерирует Prisma Client
- ✅ Запускает Backend на порту 3001
- ✅ Тестирует API endpoints

**Время запуска:** ~30 секунд

---

### 2️⃣ **start-frontend-only.ps1** - Запуск только Frontend
Запускает frontend с проверкой backend

```powershell
.\start-frontend-only.ps1
```

**Что делает:**
- ✅ Проверяет, запущен ли backend
- ✅ Останавливает старые процессы frontend
- ✅ Запускает Frontend на порту 3000
- ⏱️ Ждет 40 секунд для компиляции

**Важно:** Backend должен быть запущен первым!

**Время запуска:** ~40-60 секунд

---

### 3️⃣ **start-all.ps1** - Запуск всего сразу
Запускает и backend, и frontend одновременно

```powershell
.\start-all.ps1
```

**Рекомендация:** Для стабильной работы лучше использовать раздельные скрипты (1️⃣ + 2️⃣)

---

## 🎯 Рекомендуемый способ запуска

### Вариант A: Два отдельных скрипта (надежнее)

```powershell
# Терминал 1 (PowerShell): Backend
.\start-backend-only.ps1

# Подождите "Backend started successfully!"

# Терминал 2 (PowerShell): Frontend  
.\start-frontend-only.ps1
```

### Вариант B: Backend скрипт + Ручной frontend

```powershell
# Терминал 1: Backend
.\start-backend-only.ps1

# Терминал 2: Frontend вручную
cd frontend
npm run dev
```

---

## 📊 Проверка статуса

### Проверить что запущено:

```powershell
# Backend (должен вернуть True)
Test-NetConnection localhost -Port 3001 -InformationLevel Quiet

# Frontend (должен вернуть True)
Test-NetConnection localhost -Port 3000 -InformationLevel Quiet

# Протестировать API
Invoke-RestMethod http://localhost:3001/locations
```

### Открыть в браузере:

```
Backend API:  http://localhost:3001
Frontend App: http://localhost:3000
```

---

## 🛑 Остановка сервисов

### Остановить все процессы Node.js:

```powershell
Get-Process node -ErrorAction SilentlyContinue | Stop-Process -Force
```

### Остановить только Backend:

```powershell
Get-NetTCPConnection -LocalPort 3001 | ForEach-Object {
    Stop-Process -Id $_.OwningProcess -Force
}
```

### Остановить только Frontend:

```powershell
Get-NetTCPConnection -LocalPort 3000 | ForEach-Object {
    Stop-Process -Id $_.OwningProcess -Force
}
```

---

## 🐛 Troubleshooting

### Backend не запускается

1. Проверьте x64 Node.js:
   ```powershell
   Test-Path "$env:USERPROFILE\nodejs-x64\node.exe"
   ```

2. Установите x64 Node.js:
   ```powershell
   cd backend
   .\install-node-x64.ps1
   ```

3. Проверьте `.env`:
   ```powershell
   Test-Path backend\.env
   ```

### Frontend не запускается

1. Убедитесь, что backend запущен:
   ```powershell
   Test-NetConnection localhost -Port 3001 -InformationLevel Quiet
   ```

2. Запустите frontend вручную:
   ```powershell
   cd frontend
   npm run dev
   ```

3. Подождите 1-2 минуты для компиляции Vite

### Ошибка "Port already in use"

```powershell
# Найти процесс на порту 3001
Get-NetTCPConnection -LocalPort 3001 | Select-Object OwningProcess

# Остановить процесс (замените PID)
Stop-Process -Id <PID> -Force
```

---

## 📚 Документация

- **[docs/QUICKSTART.md](docs/QUICKSTART.md)** - Полная инструкция по установке
- **[docs/BUGS_HISTORY.md](docs/BUGS_HISTORY.md)** - История всех багов и решений
- **[README.md](README.md)** - Общая информация о проекте

---

## ⚡ Быстрая шпаргалка

```powershell
# ✅ ЗАПУСТИТЬ ВСЁ (два терминала)
# Терминал 1:
.\start-backend-only.ps1

# Терминал 2:
.\start-frontend-only.ps1

# ✅ ОСТАНОВИТЬ ВСЁ
Get-Process node | Stop-Process -Force

# ✅ ПРОВЕРИТЬ СТАТУС
Test-NetConnection localhost -Port 3001 -InformationLevel Quiet
Test-NetConnection localhost -Port 3000 -InformationLevel Quiet

# ✅ ОТКРЫТЬ В БРАУЗЕРЕ
start http://localhost:3000
```

---

**Версия:** 1.0  
**Дата:** 8 декабря 2025  
**Платформа:** Windows ARM64 + x64 Node.js

