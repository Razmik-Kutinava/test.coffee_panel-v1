# 🖼️ Настройка загрузки изображений товаров

## ✅ Что уже сделано

1. ✅ Создан `StorageService` для работы с Supabase Storage
2. ✅ Добавлен эндпоинт `POST /products/upload-image` для загрузки изображений
3. ✅ Обновлен frontend для выбора и загрузки изображений
4. ✅ Добавлено отображение изображений в списке товаров
5. ✅ Реализовано автоматическое удаление изображений при удалении/обновлении товара

## 📋 Что нужно сделать вручную

### 1. Установить зависимости

```bash
cd backend
npm install
```

Это установит `@types/multer`, который был добавлен в `package.json`.

### 2. Настроить Supabase Storage

Следуйте инструкции в файле: **`backend/SUPABASE_STORAGE_SETUP.md`**

**Кратко:**
1. Создайте bucket `product-images` в Supabase Dashboard (Storage → New bucket)
2. Сделайте bucket публичным
3. Настройте политики доступа (см. инструкцию)
4. Получите `SUPABASE_URL` и `SUPABASE_SERVICE_KEY` из Settings → API
5. Добавьте в `backend/.env`:
   ```env
   SUPABASE_URL=https://your-project.supabase.co
   SUPABASE_SERVICE_KEY=your-service-role-key
   ```

### 3. Перезапустить серверы

После настройки переменных окружения:

```bash
# Backend
cd backend
npm run start:dev

# Frontend (в другом терминале)
cd frontend
npm run dev
```

## 🎯 Как использовать

1. Откройте приложение в браузере
2. Перейдите в раздел "Каталог" → "Товары"
3. Нажмите "Добавить товар"
4. В форме создания товара выберите изображение через поле "Изображение товара"
5. Вы увидите превью изображения
6. Заполните остальные поля и нажмите "Создать"
7. Изображение загрузится в Supabase Storage, URL сохранится в БД
8. Изображение появится в списке товаров

## 🔍 Проверка работы

1. **Проверьте логи backend** при загрузке:
   - Должно быть: `✅ Изображение загружено: https://...`
   - Если ошибка: проверьте переменные окружения

2. **Проверьте Supabase Dashboard**:
   - Storage → `product-images` → должны появиться файлы

3. **Проверьте БД**:
   - В таблице `Product` поле `imageUrl` должно содержать URL

## ⚠️ Важные замечания

- **Размер файла**: максимум 5MB
- **Типы файлов**: JPEG, PNG, WebP, GIF
- **Публичный доступ**: bucket должен быть публичным для отображения изображений
- **Безопасность**: в production настройте правильные политики доступа

## 🐛 Troubleshooting

### Ошибка: "Supabase Storage не настроен"
→ Проверьте переменные `SUPABASE_URL` и `SUPABASE_SERVICE_KEY` в `.env`

### Ошибка: "Bucket not found"
→ Убедитесь, что bucket `product-images` создан и имя точно совпадает

### Изображения не отображаются
→ Проверьте, что bucket помечен как **Public** в Supabase Dashboard

### Ошибка при загрузке файла
→ Проверьте размер файла (макс 5MB) и тип (только изображения)

---

**Подробная инструкция**: `backend/SUPABASE_STORAGE_SETUP.md`

