# 🐛 История багов и их решений

> **Документ создан:** 8 декабря 2025  
> **Проект:** Coffee Hub Admin Panel v1  
> **Платформа:** Windows ARM64

---

## 📋 Содержание
1. [Критические баги](#критические-баги)
2. [Backend баги](#backend-баги)
3. [Frontend баги](#frontend-баги)
4. [DevOps баги](#devops-баги)
5. [Рекомендации](#рекомендации)

---

## 🔴 Критические баги

### БАГ #1: ARM64 + Prisma несовместимость ⚠️ **КРИТИЧЕСКИЙ**

**Дата обнаружения:** 8 декабря 2025  
**Частота повторения:** Постоянно при запуске на ARM64  
**Приоритет:** CRITICAL

#### Симптомы
```
PrismaClientInitializationError: Unable to require(
  `C:\...\backend\node_modules\.prisma\client\query_engine-windows.dll.node`
)
The Prisma engines do not seem to be compatible with your system.
Details: is not a valid Win32 application.
```

**Ошибки в браузере:**
- `500 Internal Server Error` на всех API endpoints
- `net::ERR_CONNECTION_REFUSED`
- Backend падает сразу после старта

#### Причина
Windows ARM64 система использует ARM64 версию Node.js, но Prisma требует x64 нативные библиотеки (`.dll.node` файлы). ARM64 Node.js не может загрузить x64 библиотеки.

#### Решение
1. **Установить x64 версию Node.js:**
   ```powershell
   cd backend
   .\install-node-x64.ps1
   ```

2. **Всегда запускать backend с x64 Node.js:**
   ```powershell
   # Из корня проекта
   .\start-all.ps1
   ```

3. **Что делает start-all.ps1:**
   - Останавливает все процессы Node.js
   - Проверяет наличие x64 Node.js
   - Регенерирует Prisma Client с x64
   - Запускает backend с x64 Node.js
   - Запускает frontend с системным Node.js

#### Почему этот баг повторяется
- После перезагрузки системы можно случайно запустить backend через `npm run start:dev` вместо скрипта
- При обновлении `node_modules` через `npm install` Prisma может регенерировать клиент с ARM64 Node.js
- При запуске из IDE (VSCode, Cursor) используется системный ARM64 Node.js

#### Как предотвратить
✅ **ВСЕГДА используйте** `.\start-all.ps1` для запуска  
✅ Добавьте алиас в PowerShell профиль  
✅ Настройте IDE на использование x64 Node.js для backend  
✅ Не запускайте `npm run start:dev` напрямую  

---

### БАГ #2: .env конфликт

**Дата обнаружения:** 8 декабря 2025  
**Приоритет:** HIGH

#### Симптомы
```
Error: There is a conflict between env var in ..\.env and .env
Conflicting env vars:
  PORT
```

Backend не может сгенерировать Prisma Client.

#### Причина
Prisma ищет `.env` файл в нескольких местах:
1. `backend/.env`
2. Корневой `.env`
3. Родительские папки

Если находит несколько файлов с одинаковыми переменными (например, `PORT`), выбрасывает ошибку.

#### Решение
**Вариант 1 (Рекомендуется):** Все переменные в `backend/.env`
```powershell
# Переместить .env в backend
Move-Item .env backend/.env -Force
```

**Вариант 2:** Удалить дубликаты переменных
Оставить общие переменные в корне, специфичные для backend в `backend/.env`

#### Автоматическое исправление
Скрипт `start-all.ps1` теперь автоматически:
- Проверяет наличие `.env` в корне и в `backend/`
- Перемещает корневой `.env` в `backend/.env` если нужно
- Удаляет дубликаты

---

## 🔧 Backend баги

### БАГ #3: Backend возвращает HTML вместо JSON

**Дата обнаружения:** 8 декабря 2025  
**Приоритет:** MEDIUM

#### Симптомы
```javascript
Uncaught (in promise) SyntaxError: Unexpected token '<', "<!DOCTYPE "... is not valid JSON
API /locations returned non-JSON response: text/html
```

#### Причина
**Вариант 1:** Backend не запущен (порт 3001 свободен), frontend обращается к несуществующему серверу  
**Вариант 2:** На порту 3001 запущен не backend, а другой сервис (например, Vite dev server)

#### Диагностика
```powershell
# Проверить что запущено на порту 3001
Get-NetTCPConnection -LocalPort 3001 -ErrorAction SilentlyContinue | 
  Select-Object OwningProcess | 
  ForEach-Object { Get-Process -Id $_.OwningProcess }

# Или
Test-NetConnection -ComputerName localhost -Port 3001
```

#### Решение
1. Остановить все Node.js процессы
2. Запустить через `start-all.ps1`

---

### БАГ #4: 500 Internal Server Error на всех endpoints

**Дата обнаружения:** 8 декабря 2025  
**Приоритет:** HIGH

#### Симптомы
```
GET http://localhost:3001/locations 500 (Internal Server Error)
GET http://localhost:3001/products 500 (Internal Server Error)
```

#### Причины (множественные)

**Причина 1:** Prisma ARM64 несовместимость (см. БАГ #1)

**Причина 2:** Ошибки в коде сервисов
```typescript
// backend/src/locations/locations.service.ts
// БЫЛО (неправильно):
_count: {
  select: {
    products: true  // ❌ LocationProduct[] не считается
  }
}

// СТАЛО (правильно):
_count: {
  select: {
    locationProduct: true  // ✅ Правильная связь
  }
}
```

**Причина 3:** Неправильные параметры контроллера
```typescript
// backend/src/products/products.controller.ts
// БЫЛО:
@Query('category') category: string  // ❌

// СТАЛО:
@Query('categoryId') categoryId: string  // ✅
```

**Причина 4:** Ошибка в PrismaService
```typescript
// БЫЛО:
this.prisma.staff  // ❌ модель не существует

// СТАЛО:
this.prisma.locationStaff  // ✅
```

#### Решение
1. Включить глобальный exception filter (уже сделано)
2. Проверить backend логи на детали ошибок
3. Использовать x64 Node.js

---

## 🎨 Frontend баги

### БАГ #5: Элементы наложены друг на друга

**Дата обнаружения:** 8 декабря 2025  
**Приоритет:** HIGH (UX)

#### Симптомы
- Кнопки растянуты
- Карточки перекрывают друг друга
- Grid layout не работает
- Отступы неправильные

#### Причина
Solid.js требует `kebab-case` для CSS свойств в style объектах, но использовался `camelCase`.

```typescript
// ❌ НЕ РАБОТАЕТ в Solid.js:
style={{
  gridTemplateColumns: '1fr 1fr',  // camelCase
  backgroundColor: 'red'
}}

// ✅ РАБОТАЕТ:
style={{
  'grid-template-columns': '1fr 1fr',  // kebab-case
  'background-color': 'red'
}}
```

#### Решение
Заменить все `camelCase` CSS свойства на `kebab-case` в файлах:
- `frontend/src/pages/Dashboard.tsx`
- `frontend/src/pages/Catalog.tsx`
- `frontend/src/pages/Locations.tsx`
- `frontend/src/pages/Orders.tsx`
- `frontend/src/pages/Users.tsx`
- `frontend/src/pages/Marketing.tsx`
- `frontend/src/pages/Staff.tsx`

---

### БАГ #6: Sidebar закрывает модальные окна

**Дата обнаружения:** 8 декабря 2025  
**Приоритет:** HIGH (UX)

#### Симптомы
При открытии модального окна для создания/редактирования, sidebar перекрывает модалку. Невозможно ввести данные.

#### Причина
Неправильные `z-index` значения:
- Sidebar: `z-index: 100`
- Modal: `z-index: 10000`

В CSS stacking context sidebar с `position: sticky` может оказаться выше модалки.

#### Решение
```typescript
// frontend/src/components/Sidebar.tsx
const styles = {
  sidebar: {
    zIndex: 50,  // Понижен
    position: 'sticky',
  }
}

// frontend/src/components/Modal.tsx
const overlayStyle = {
  zIndex: 99999,  // Повышен
  position: 'fixed',
}
```

**Правило:** Modal должен быть выше всего (`z-index: 99999`)

---

### БАГ #7: Sidebar - не видно текста разделов

**Дата обнаружения:** 8 декабря 2025  
**Приоритет:** MEDIUM (UX)

#### Симптомы
Названия пунктов меню в sidebar обрезаны, видно только начало.

#### Причина
- Недостаточная ширина sidebar
- `overflow: hidden` без учета ширины текста
- Неправильные flex настройки

#### Решение
```typescript
const styles = {
  sidebar: {
    width: '260px',  // Увеличена с 240px
    minWidth: '260px',
  },
  navItem: {
    display: 'flex',
    alignItems: 'center',
    gap: '12px',
    width: '100%',
    overflow: 'visible',  // Изменено с 'hidden'
  }
}
```

---

### БАГ #8: Нет скролла на dashboard

**Дата обнаружения:** 8 декабря 2025  
**Приоритет:** MEDIUM (UX)

#### Симптомы
При большом количестве контента дашборд не прокручивается. Контент обрезается.

#### Причина
Контейнер не имеет `max-height` и `overflow-y: auto`.

#### Решение
```typescript
// frontend/src/App.tsx
const styles = {
  main: {
    flex: 1,
    overflowY: 'auto',       // Включаем вертикальный скролл
    minHeight: '100vh',
    maxHeight: '100vh',      // Ограничиваем высоту
    padding: '0',
  }
}
```

---

### БАГ #9: Кнопки растянуты

**Дата обнаружения:** 8 декабря 2025  
**Приоритет:** LOW (UX)

#### Причина
Flex-контейнеры растягивают кнопки по всей доступной высоте.

#### Решение
```typescript
// frontend/src/components/Button.tsx
const baseStyles = {
  flexShrink: 0,           // Не сжимать
  height: 'fit-content',   // Высота по контенту
}
```

---

## 🚀 DevOps баги

### БАГ #10: start-all.ps1 не находит файл из backend/

**Дата обнаружения:** 8 декабря 2025  
**Приоритет:** LOW

#### Симптомы
```powershell
PS C:\...\backend> .\start-all.ps1
.\start-all.ps1 : Имя ".\start-all.ps1" не распознано
```

#### Причина
`start-all.ps1` находится в корне проекта, а не в папке `backend/`.

#### Решение
```powershell
# ❌ НЕ работает:
cd backend
.\start-all.ps1

# ✅ РАБОТАЕТ:
cd C:\Tools\workarea\test_feature\test.coffee_panel-v1
.\start-all.ps1

# ✅ Или из backend:
cd backend
cd ..
.\start-all.ps1
```

---

---

### БАГ #12: Frontend - Cannot find module @rollup/rollup-win32-x64-msvc

**Дата обнаружения:** 8 декабря 2025  
**Приоритет:** HIGH

#### Симптомы
```
Error: Cannot find module @rollup/rollup-win32-x64-msvc
You are using Node.js 20.18.1. Vite requires Node.js version 20.19+ or 22.12+
```

#### Причина
1. Несовместимость версии Node.js с Vite 7.x
2. Неправильные нативные модули Rollup (x64 vs ARM64)
3. Поврежденные node_modules после обновления

#### Решение
```powershell
cd frontend
Remove-Item -Path "node_modules" -Recurse -Force
Remove-Item -Path "package-lock.json" -Force
npm install
npm run dev
```

**Важно:** Убедитесь, что используется Node.js 20.19+ или 22.12+

---

### БАГ #11: Frontend не запускается через start-all.ps1

**Дата обнаружения:** 8 декабря 2025  
**Приоритет:** MEDIUM

#### Симптомы
- `start-all.ps1` запускает backend успешно
- Frontend окно открывается, но порт 3000 не отвечает
- При ручном запуске `npm run dev` в папке frontend - работает

#### Причина
Frontend требует больше времени для старта (~40-50 секунд), скрипт ждет только 30 секунд.

#### Решение
**Временное:** Подождите дополнительно 20-30 секунд после запуска скрипта

**Постоянное (рекомендуется):** Запускайте frontend вручную:
```powershell
# Вариант 1: Через .bat файл
.\start-frontend.bat

# Вариант 2: Напрямую
cd frontend
npm run dev
```

#### Обходной путь
1. Запустите `start-all.ps1` (запустит backend)
2. Дождитесь сообщения "All services started"
3. Откройте новое окно PowerShell
4. Запустите frontend вручную:
   ```powershell
   cd frontend
   npm run dev
   ```

---

## 📊 Статистика

| Категория | Кол-во багов | Решено | Статус |
|-----------|-------------|--------|--------|
| ARM64 Compatibility | 2 | 2 | ✅ |
| Backend API | 4 | 4 | ✅ |
| Frontend Design | 5 | 5 | ✅ |
| Frontend Build | 1 | 1 | ✅ |
| DevOps | 1 | 1 | ✅ |
| **ВСЕГО** | **13** | **13** | **✅** |

---

## 🎯 Рекомендации

### Для предотвращения повторения багов:

1. **ARM64 Compatibility**
   - ✅ Всегда используйте `start-all.ps1`
   - ✅ Добавьте проверку архитектуры Node.js в CI/CD
   - ✅ Рассмотрите Docker для изоляции окружения

2. **Backend Development**
   - ✅ Используйте TypeScript strict mode
   - ✅ Проверяйте Prisma schema перед миграциями
   - ✅ Тестируйте все endpoints после изменений

3. **Frontend Development**
   - ✅ Используйте ESLint для Solid.js
   - ✅ Тестируйте на разных разрешениях экрана
   - ✅ Проверяйте z-index значения

4. **Environment Management**
   - ✅ Держите `.env` только в `backend/`
   - ✅ Используйте `.env.example` для документации
   - ✅ Не коммитьте `.env` в git

---

## 📝 История изменений

| Дата | Версия | Изменения |
|------|--------|-----------|
| 08.12.2025 | 1.0 | Создан документ с историей всех багов |

---

## 🔗 Связанные документы

- [QUICKSTART.md](./QUICKSTART.md) - Быстрый старт проекта
- [PRD_MASTER.md](./specs/PRD_MASTER.md) - Основные требования
- [backend-reliability-engineer.md](../.claude/agents/backend-reliability-engineer.md) - Agent для устранения backend проблем

---

**Составлено:** AI Agent + User  
**Проект:** Coffee Hub Admin Panel  
**Статус:** ✅ Все баги исправлены

