# 🚀 Quick Start Guide

> **Быстрый запуск Coffee Hub Admin Panel на Windows ARM64**

---

## ⚡ Быстрый старт (TL;DR)

```powershell
# 1. Клонируйте проект
git clone <repo-url>
cd test.coffee_panel-v1

# 2. Установите зависимости
cd backend
npm install
.\install-node-x64.ps1  # Важно для ARM64!
cd ../frontend
npm install
cd ..

# 3. Настройте окружение
# Создайте backend/.env (см. ниже)

# 4. Запустите проект
.\start-all.ps1

# 5. Откройте браузер
# http://localhost:3000
```

**Готово!** 🎉

---

## 📋 Подробная инструкция

### Шаг 1: Требования

- **Windows ARM64** (например, Windows 11 на Surface Pro X)
- **Node.js** v18+ (ARM64 версия для frontend)
- **PostgreSQL** (или Supabase)
- **PowerShell** 5.1+

### Шаг 2: Установка Node.js x64 для Backend

⚠️ **КРИТИЧЕСКИ ВАЖНО для Windows ARM64!**

```powershell
cd backend
.\install-node-x64.ps1
```

**Что делает скрипт:**
- Скачивает portable Node.js x64 v18.20.5
- Устанавливает в `%USERPROFILE%\nodejs-x64`
- Не затрагивает системный Node.js

**Почему это нужно:**
Backend использует Prisma ORM, который требует x64 нативные библиотеки. ARM64 Node.js не может их загрузить.

### Шаг 3: Установка зависимостей

```powershell
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install

# Вернуться в корень
cd ..
```

### Шаг 4: Настройка .env

Создайте файл `backend/.env`:

```env
# Database (Supabase или локальный PostgreSQL)
DATABASE_URL="postgresql://postgres:password@host:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:password@host:5432/postgres"

# Server
PORT=3001

# Frontend URL (для CORS)
FRONTEND_URL="http://localhost:3000"
```

**Для Supabase:**
1. Зайдите в Dashboard → Project Settings → Database
2. Скопируйте `Connection pooling` URL → `DATABASE_URL`
3. Скопируйте `Connection string` URL → `DIRECT_URL`

### Шаг 5: Миграции базы данных

```powershell
cd backend

# Применить миграции
npm run prisma:push

# Или сгенерировать и применить
npm run prisma:generate
npm run prisma:push

cd ..
```

### Шаг 6: Запуск проекта

#### Вариант А: Автоматический запуск (рекомендуется для backend)

```powershell
# Из корня проекта
.\start-all.ps1
```

**Скрипт автоматически:**
1. ✅ Останавливает старые Node.js процессы
2. ✅ Проверяет x64 Node.js
3. ✅ Исправляет .env конфликты
4. ✅ Регенерирует Prisma Client с x64
5. ✅ Запускает Backend (порт 3001)
6. ✅ Запускает Frontend (порт 3000)
7. ✅ Тестирует API endpoints

**Ждите ~30 секунд** для backend, **+20-30 секунд** для frontend.

⚠️ **Известная проблема:** Frontend может требовать дополнительное время для старта. Если через минуту frontend не отвечает, используйте Вариант Б.

#### Вариант Б: Ручной запуск frontend (надежнее)

```powershell
# 1. Запустите backend через скрипт
.\start-all.ps1

# 2. Дождитесь сообщения "All services started"

# 3. Откройте НОВОЕ окно PowerShell и запустите frontend
cd frontend
npm run dev
```

Этот способ гарантирует, что frontend запустится корректно.

### Шаг 7: Откройте браузер

```
Frontend: http://localhost:3000
Backend API: http://localhost:3001
```

---

## 🛠️ Альтернативные команды

### Запуск только Backend

```powershell
cd backend
.\start-x64.ps1
```

### Запуск только Frontend

```powershell
cd frontend
npm run dev
```

### Ручной запуск Backend (для отладки)

```powershell
cd backend
$nodeX64 = "$env:USERPROFILE\nodejs-x64"
$env:Path = "$nodeX64;$env:Path"
& "$nodeX64\npm.cmd" run start:dev
```

---

## 🐛 Troubleshooting

### Проблема: "PrismaClientInitializationError: is not a valid Win32 application"

**Решение:**
```powershell
# Остановить все
Get-Process node -ErrorAction SilentlyContinue | Stop-Process -Force

# Запустить через скрипт
.\start-all.ps1
```

**Причина:** Backend запущен с ARM64 Node.js вместо x64.

---

### Проблема: "API returned non-JSON response: text/html"

**Диагностика:**
```powershell
# Проверить что на порту 3001
Test-NetConnection -ComputerName localhost -Port 3001
```

**Решение:**
```powershell
# Перезапустить
.\start-all.ps1
```

**Причины:**
- Backend не запущен
- Backend упал из-за ошибки
- На порту 3001 другой процесс

---

### Проблема: "There is a conflict between env var in .env"

**Решение:**
```powershell
# Если есть .env в корне и в backend/
Move-Item .env backend/.env -Force

# Или запустите start-all.ps1 (исправит автоматически)
.\start-all.ps1
```

**Причина:** Prisma находит `.env` в нескольких местах.

---

### Проблема: Frontend показывает ошибки дизайна

**Решение:**
```powershell
# Очистить кеш браузера (Ctrl+Shift+Delete)
# Или жесткая перезагрузка (Ctrl+F5)

# Перезапустить frontend
cd frontend
npm run dev
```

---

### Проблема: Backend не запускается

**Диагностика:**
```powershell
# Проверить логи
cd backend
npm run start:dev
# Смотрите ошибки в консоли
```

**Частые причины:**
1. ❌ Нет `backend/.env`
2. ❌ Неправильный DATABASE_URL
3. ❌ База данных недоступна
4. ❌ Используется ARM64 Node.js

---

## 📊 Проверка работоспособности

### Тест Backend API

```powershell
# Локации
Invoke-RestMethod -Uri "http://localhost:3001/locations"

# Продукты
Invoke-RestMethod -Uri "http://localhost:3001/products"

# Dashboard
Invoke-RestMethod -Uri "http://localhost:3001/dashboard"
```

### Проверка Node.js архитектуры

```powershell
# Системный Node.js (должен быть arm64)
node -p "process.arch"

# Backend Node.js (должен быть x64)
& "$env:USERPROFILE\nodejs-x64\node.exe" -p "process.arch"
```

---

## 🎯 Рекомендации

### ✅ DO (Делайте так)

- ✅ Используйте `.\start-all.ps1` для запуска
- ✅ Держите `.env` в `backend/` папке
- ✅ Проверяйте логи при ошибках
- ✅ Используйте Chrome DevTools для отладки frontend

### ❌ DON'T (Не делайте так)

- ❌ Не запускайте backend через `npm run start:dev` напрямую
- ❌ Не держите `.env` в корне проекта
- ❌ Не используйте `npm install` в backend без последующего `npx prisma generate` с x64
- ❌ Не коммитьте `.env` в git

---

## 📁 Структура проекта

```
test.coffee_panel-v1/
├── backend/                  # NestJS Backend
│   ├── src/                  # Исходный код
│   ├── prisma/               # Prisma schema
│   ├── .env                  # ⚠️ Переменные окружения (не в git)
│   ├── install-node-x64.ps1  # Установка x64 Node.js
│   └── start-x64.ps1         # Запуск с x64
│
├── frontend/                 # Solid.js Frontend
│   ├── src/                  # Исходный код
│   │   ├── components/       # Компоненты
│   │   ├── pages/            # Страницы
│   │   └── App.tsx           # Главный компонент
│   └── package.json
│
├── docs/                     # Документация
│   ├── BUGS_HISTORY.md       # История багов
│   ├── QUICKSTART.md         # Этот файл
│   └── specs/                # Спецификации
│
├── start-all.ps1             # ⭐ Главный скрипт запуска
└── README.md
```

---

## 🔗 Полезные ссылки

- **Prisma Docs:** https://www.prisma.io/docs
- **NestJS Docs:** https://docs.nestjs.com
- **Solid.js Docs:** https://www.solidjs.com/docs
- **Supabase Docs:** https://supabase.com/docs

---

## 📝 FAQ

### Q: Нужно ли устанавливать x64 Node.js глобально?

**A:** Нет! `install-node-x64.ps1` устанавливает portable версию в `%USERPROFILE%\nodejs-x64`. Она не затрагивает системный Node.js.

### Q: Можно ли использовать Docker?

**A:** Да, но для Windows ARM64 могут быть проблемы с эмуляцией x64. Native setup проще.

### Q: Почему backend не может использовать ARM64 Node.js?

**A:** Prisma использует нативные библиотеки (`.dll.node`), скомпилированные для x64. ARM64 Node.js не может загрузить x64 библиотеки из-за архитектурной несовместимости.

### Q: Как обновить зависимости?

**A:**
```powershell
# Backend
cd backend
npm update
.\start-x64.ps1  # Важно! Регенерирует Prisma

# Frontend
cd frontend
npm update
npm run dev
```

---

**Версия:** 1.0  
**Дата:** 8 декабря 2025  
**Составлено:** AI Agent + User

**Статус:** ✅ Протестировано на Windows ARM64

